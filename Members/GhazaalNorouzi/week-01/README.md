1. Create an empty array to store the result.
2. Iterate through the numbers from `1` to `n`.
3. For each number:
   - Check if it is divisible by `15` (`3 × 5`). If true, add `"FizzBuzz"`.
   - Otherwise, check if it is divisible by `5`. If true, add `"Buzz"`.
   - Otherwise, check if it is divisible by `3`. If true, add `"Fizz"`.
   - Otherwise, add the number as a string.
4. Return the resulting array.
