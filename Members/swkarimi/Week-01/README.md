## Complexity

| Complexity | Value | Explanation |
|------------|-------|-------------|
| Time | `O(n)` | The algorithm iterates from `1` to `n` exactly once. |
| Space | `O(n)` | The algorithm allocates an array of size `n`. |
