# Binary Search

* Language: JavaScript

* Idea:
  I solved this problem using two pointers that point to the first and last elements of the array. Then, I check the middle value against the target and move the pointers based on result.

## Complexity

* **Time Complexity:** `O(log n)`
* **Space Complexity:** `O(1)`
